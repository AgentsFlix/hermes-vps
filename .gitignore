# estado local do aluno: nunca sobe
config/hermes-vps.env
config/hermes-vps.env.bak
config/acesso.local
config/senha.local
config/.estado
config/.preflight.ok
config/*.bak
# gerado pelos testes
tests/.tmp/
.DS_Store
